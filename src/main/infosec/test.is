fun main(argv : string[]) {
    if #argv > 0 {
        for var i = 0; i < #argv; i++; {
            print(argv[i]);
        }
    }
    if #argv == 0  {
        print("No arguments to report");
    }
}
